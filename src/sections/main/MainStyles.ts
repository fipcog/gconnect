import styled from "styled-components"

const Main = styled.main`
    display: flex;
    /* flex-direction: column; */

    width: 100%;
    height: 100%;

    padding-bottom: 25px;
    overflow: hidden;
`

export const S = {
    Main,
}