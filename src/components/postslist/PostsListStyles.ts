import styled from "styled-components";


const PostsList = styled.ul`
    display: flex;
    flex-direction: column;

    width: 100%;
    height: 150px;

    overflow: hidden;
    overflow-y: auto;
`

export const S = {
    PostsList,
}